# UNGRUND — App architecture

> Живой модульный SuperCollider-скелет системы UNGRUND. Каждый синт-слой
> и модуль итерируется ОТДЕЛЬНО. Скелет загружается и звучит из одного
> файла, оставляя чистый сервер после Cmd-. .

---

## 1. Что такое UNGRUND (кратко)

UNGRUND — система абсолютной электронной музыки после Титченса (Tietchens).
Звук трактуется как *субстрат*, а не носитель: весь материал порождается
синтезом из первопринципов (никаких записей/сэмплов, Аксиома II),
композиция строится *блоками*, а не аркой (Аксиома IV), и сама структура
выставляется уху как измерение, а не как выражение. Связующая аксиома:
«ни один звук не оправдан чувством; каждый звук оправдан своей позицией в
калибруемом процессе, слышимом ухом».

Словарь системы — восемь звуковых классов, названных греческими буквами
`alpha`…`theta` (по порядку §C в `docs/reference_v2.md`):

| Класс     | §C    | Премиса (исходное имя UNGRUND)            |
|-----------|-------|--------------------------------------------|
| `alpha`   | §C.1  | sustained sine / Grund (`monade`)          |
| `beta`    | §C.2  | broadband impulse (`riß`)                  |
| `gamma`   | §C.3  | granular cluster of sines (`flockung`)     |
| `delta`   | §C.4  | narrow-band noise (`binde`)                |
| `epsilon` | §C.5  | drifting comb-filtered noise (`schliere`)  |
| `zeta`    | §C.6* | vinyl/tape surface noise (`kratzer`)       |
| `eta`     | §C.7  | composed silence (`zäsur`)                 |
| `theta`   | §C.8  | ultrasonic shimmer / ceiling (`firnis`)    |

\* Премиса `zeta` ВЫТЕСНЕНА редизайном 2026-06: вместо «сухого
ВЧ-скрежета» (§C.6 kratzer) слой реализует поверхность изношенного
винила / старой ленты по измеренному референсу
`docs/zeta_texture_reference.md` (спека — `docs/task.md`). Это поправка
системы (amendment); пометка `> superseded` в §C.6 `reference_v2.md` —
открытый пункт бэклога (`docs/improvements.md` №9).

В коде и в этом документе слои называются строго греческими буквами; исходные
немецкие имена (`monade`, `riß`, …) сохраняются только как ссылка на §C
`docs/reference_v2.md`, где описана премиса синтеза каждого класса.
Тембральная редакция 2026 (§K) допускает реверб и делей как операции синтеза
(не как «комнату»): тёплый, обволакивающий, внутренне подвижный материал.

Имена слоёв — ASCII (греческие буквы записаны латиницей: `alpha`, `beta`, …),
поэтому имя файла, символ `Ndef(\<class>)` и спецификация `~spec_<class>`
совпадают (`alpha.scd` → `Ndef(\alpha)` → `~spec_alpha`).

---

## 2. Семислойная модель приложения

Движок — **JITLib** (`Ndef`/`Tdef`/`MIDIdef`): каждый слой/модуль можно
горячо заменить и итерировать независимо, просто перевыполнив его файл.

| № | Файл | Слой | Ответственность |
|---|------|------|-----------------|
| 1 | `App/1_app.scd` | **app** | Оркестратор + ЕДИНАЯ ПАНЕЛЬ (`~cfg_app`). Boot сервера, база-директория, аллокация шин, ВЫБОР загружаемых слоёв (`layers`), ФЛАГИ включения модулей (`enable*`), авто-пресеты, загрузка в порядке зависимостей, `s.sync`, открытие окон (микшер/виз), старт, мастер-очистка `~appCleanup` (включая секвенсорный TempoClock). |
| 2 | `App/2_mixer.scd` | **mixer** | Маршрутизация ТОЛЬКО активных слоёв (`~activeLayers`) в `~busDry`, агрегатор посылов в `~busFxSend`, мастер (LeakDC + Limiter). Сеттеры `~mixSet/~masterSet/~mixSolo/~mixUnsolo` + **окно микшера** `~openMixer` (фейдеры vol/send, solo, кнопка tuner, мастер). |
| 3 | `App/3_lfo.scd` | **lfo** | *Опционально* (`enableLfo`). Центральный реестр автоматизации. Таблица строк (target / param / shape / rate / range); движок создаёт kr-Ndef на строку и маппит на цель. Идемпотентность — по собственному реестру `~lfoActive` (unmap + clear исчезнувших строк). |
| 4 | `App/4_sequencer.scd` | **sequencer** | *Опционально* (`enableSequencer`). Блочный таймлайн + Tdef на TempoClock (один клок на сессию, переиспользуется при перезагрузке). Громкости слоёв ставит НАПРЯМУЮ в Ndef-роутеры (`~seqSetVol`), не трогая сведение в `~cfg_mixer`. Экспортирует `~seqCurrentBlock`/`~seqStartTime` для виз-окна. |
| 5 | `App/5_control.scd` | **control** | *Опционально* (`enableControl`). MIDI CC→цель; обобщённый MIDIdef. Nil-безопасен к выключенному LFO (`~lfoKeys`). |
| 6 | `App/6_effects.scd` | **effects** | *Опционально* (`enableEffects`). Эффект-прокси: tape reverb (§K.1) + ping-pong delay (§K.2). Читают `~busFxSend`, пишут `~busFxReturn`. |
| 7 | `App/7_viz.scd` | **viz** | *Опционально* (`enableVizGui`). Окно визуализации: таймлайн секвенсора (блоки + playhead) и живые значения LFO. Полностью nil-безопасно при выключенных seq/LFO. |
| — | `App/synths/*.scd` | **synths** | Восемь звуковых классов, по одному файлу. Каждый — `Ndef(\<class>)` с CONFIG-блоком сверху. Контракт файла — `docs/layer_contract.md`. |

Эффекты концептуально — седьмой слой; нумерация файлов (1–7 + synths/)
отражает *концептуальный* порядок пользователя, тогда как `1_app.scd`
грузит их в *порядке зависимостей* (см. §5).

**ФАЗА САУНД-ДИЗАЙНА (умолчания `~cfg_app`).** Сейчас задача — отстроить все
параметры всех звуков в микшере и закончить саунд-дизайн ДО работы над
секвенсором/автоматизацией/MIDI. Поэтому по умолчанию: эффекты + микшер +
окно микшера ВКЛ; LFO, секвенсор, MIDI, автозапуск — ВЫКЛ. На голом запуске
`1_app.scd` все слои звучат сразу на уровнях микшера, открыто окно сведе́ния.
Включить любой модуль = один флаг `enable*` (или `autostart`) в `~cfg_app`.

---

## 3. Карта файлов

```
App/
  0_lib.scd          инфра: specs/значения/пресеты/tuner-GUI/рандом
  1_app.scd          оркестратор + ~cfg_app: boot, выбор слоёв, флаги, load, окна, start, cleanup
  2_mixer.scd        маршрутизация активных слоёв, посылы, мастер + окно микшера (~openMixer)
  3_lfo.scd          реестр автоматизации          (опц., enableLfo)
  4_sequencer.scd    блочный таймлайн (Tdef)        (опц., enableSequencer)
  5_control.scd      MIDI CC -> цель                (опц., enableControl)
  6_effects.scd      tape reverb + ping-pong delay  (опц., enableEffects)
  7_viz.scd          окно визуализации seq + LFO    (опц., enableVizGui)
  presets/<layer>/   пресеты слоёв (default.scd авто-грузится при autoPreset)
  synths/
    alpha.scd     1/8  РЕАЛЬНЫЙ  (панель ~spec_alpha, 27 параметров; порт из Tests/test_monade.scd)
    beta.scd      2/8  заглушка  (// TODO: iterate this layer)
    gamma.scd     3/8  заглушка
    delta.scd     4/8  заглушка
    epsilon.scd   5/8  заглушка
    zeta.scd      6/8  РЕАЛЬНЫЙ  (панель ~spec_zeta, 26 параметров; vinyl/tape поверхность)
    eta.scd       7/8  РЕАЛЬНЫЙ по определению (тишина — Silent.ar)
    theta.scd     8/8  заглушка

.claude/Tools/       (вне git: политика «только App/ и docs/»)
  parse_check.scd    компиляция .scd БЕЗ выполнения (String:compile)
  render_layer.scd   рендер одного слоя в wav без вывода на колонки
  render_measure.sh  рендер + измерение ffmpeg (astats, crest, полосные RMS)
```

---

## 4. Сигнал-флоу

```
   synths (Ndef)             mixer routers              mixer master        hardware
 ┌────────────────┐        ┌────────────────┐                                out 0/1
 │ Ndef(\alpha)   │──ar──▶ │ Ndef(\mix_<L>) │──vol──▶ ~busDry ────┐
 │ Ndef(\beta)    │        └────────────────┘                     ▼
 │ Ndef(\gamma)   │        ┌────────────────┐          ┌────────────────────┐
 │ Ndef(\delta)   │──ar──▶ │ Ndef(\fxsend)  │          │   Ndef(\master)    │──▶ out
 │ Ndef(\epsilon) │        │  Σ send_<L>    │─Replace▶ │  dry + fxReturn,   │
 │ Ndef(\zeta)    │        └────────────────┘ ~busFxSend  LeakDC,           │
 │ Ndef(\eta)     │                 │              │    │  Limiter(0.95)    │
 │ Ndef(\theta)   │                 ▼              │    └────────────────────┘
 └────────────────┘        ┌──────────────────┐    │              ▲
                           │ Ndef(\tape_reverb)│◀──┘              │
                           │ Ndef(\ping_delay) │──▶ ~busFxReturn ─┘
                           └──────────────────┘
```

- Каждый синт-слой выводит свой *сухой* сигнал в свой внутренний Ndef-bus.
  Микшер читает его дважды: роутер `Ndef(\mix_<L>)` пишет масштабированный
  на `vol` сигнал в `~busDry`; агрегатор `Ndef(\fxsend)` суммирует все слои,
  масштабированные на `send_<L>`, и пишет в `~busFxSend` через `ReplaceOut`
  внутри собственного графа — РОВНО один писатель шины, без `.play`-монитора
  (Ndef-синт работает сразу после определения; монитор был бы вторым
  писателем).
- Эффекты читают `~busFxSend` через `InFeedback` и пишут в `~busFxReturn`.
- Мастер читает `~busDry` и `~busFxReturn` через `InFeedback` (независимость
  от порядка узлов), ставит `LeakDC` + `Limiter(0.95)` (§K.4), выводит в
  аппаратные каналы 0/1.

Все межслойные шины аллоцированы через `Bus.audio(s, 2)` в `1_app.scd`:
`~busDry`, `~busFxSend`, `~busFxReturn`. Никаких магических индексов.

---

## 5. Порядок загрузки (зависимости)

`1_app.scd` грузит так (с `s.sync` между батчами):

```
0_lib.scd → synths/*.scd → 6_effects.scd → 2_mixer.scd → 3_lfo.scd → 5_control.scd → 4_sequencer.scd → 7_viz.scd
```

Почему: 0_lib определяет реестр `~ungSpecs`, в который synths регистрируют
свои spec; синты и эффекты должны быть определены ДО того, как микшер их
маршрутизирует; lfo и control — ПОСЛЕ микшера (они правят его параметры);
секвенсор — ПОСЛЕДНИМ из звуковых модулей (он дирижирует громкостями слоёв);
виз-окно — самым последним (читает данные seq/LFO).

**Гейтинг по флагам.** Каждый модуль КРОМЕ synths грузится только если его
флаг `enable*` в `~cfg_app` истинен. Порядок сохраняется, пропущенные шаги
просто не выполняются. `~activeLayers` (список реально загруженных слоёв)
ставит `1_app.scd` сразу после загрузки synths — микшер и виз читают именно
его, поэтому подмножество `~cfg_app.layers = [...]` не тянет пустые Ndef'ы.
Выключенные модули НИЧЕГО не ломают: единственные внешние читатели —
`~isMapped` (0_lib, nil-guard на `~cfg_lfo`) и `~lfoKeys` (5_control, nil-guard).

---

## 6. Конвенция «CONFIG сверху»

Каждый файл начинается с явно отделённой секции
`// ===== CONFIG — edit here =====` (обычно Event/словарь правимых
значений), за которой идёт секция `// ===== LOGIC =====`. Поведение
меняется правкой ВЕРХА файла без касания логики. **Каждый параметр CONFIG
обязан реально влиять на звук** — «мёртвый CONFIG» (значение объявлено
сверху, но захардкожено литералом в теле) запрещён.

> **ВАЖНО (структура файла).** Весь файл — РОВНО ОДИН верхнеуровневый блок
> `( … )`: секции CONFIG и LOGIC живут ВНУТРИ одного блока. Это обязательно,
> потому что `1_app.scd` грузит модули через `.load`, а `.load`/интерпретатор
> выполняет только ПЕРВЫЙ блок файла — два отдельных `( )`-блока приведут к
> тому, что LOGIC молча не выполнится (а целиком файл вообще не
> скомпилируется: «unexpected '(', expecting end of file»). При итерации НЕ
> оборачивайте CONFIG и LOGIC в отдельные скобки. Исключение — `1_app.scd`:
> там блок CLEANUP внизу намеренно закомментирован (ручной сброс).

Примеры:

- **mixer** (`2_mixer.scd`): таблица `~cfg_mixer.levels`, ключ = имя слоя,
  значения `( vol: …, send: … )`. Например `alpha: ( vol: 0.85, send: 0.55 )`.
- **lfo** (`3_lfo.scd`): список `~cfg_lfo.lines`, строка =
  `( target: \alpha, param: \amp, shape: \noise, rate: 0.03, min: 0.16, max: 0.24 )`.
  Добавить автоматизацию = добавить строку.
- **control** (`5_control.scd`): `~cfg_ctrl.map`, строка =
  `( cc: 1, type: \mixVol, target: \alpha, min: 0.0, max: 1.0 )`.
  Типы: `\layer` / `\mixVol` / `\mixSend` / `\lfoRate` / `\master`.
- **sequencer** (`4_sequencer.scd`): `~cfg_seq.timeline`, блок =
  `( t: 4, active: [\theta], gone: [], set: [[\delta, \freq, 188]] )`.
- **synths** (`synths/*.scd`): два уровня CONFIG.
  - *заглушки* — `~cfg_<class>` — ключевые параметры класса с разумными
    умолчаниями (из §C / §K.4), пара `Ndef.set`.
  - *слои с панелью управления* (`alpha`, `zeta`) — `~spec_<class>`:
    ЕДИНЫЙ источник правды (список параметров: `key`/`group`/`spec`-диапазон+
    дефолт, опц. `unit`/`disp`). Из него 0_lib строит слайдеры
    (`~openTuner.(\<class>)`), два рандомайзера по группам (`\freq` —
    «частоты», `\tone` — «параметры») и round-trip-пресеты
    (`~savePreset`/`~loadPreset`, `presets/<class>/`). Слой регистрирует свой
    spec в `~ungSpecs` и применяет дефолты. Расширить управление = добавить
    строку в `~spec_<class>` И аргумент в `Ndef`. Окно тюнера безопасно к
    повторному открытию (best-effort закрытие старого окна, onClose чистит
    реестр только если окно ещё «своё»).

**Полный контракт файла слоя** (структура, регистрация, запреты на
boot/cleanup/`Out.ar`, DSP-гигиена, известные SC/Qt-ловушки) — канонически в
`docs/layer_contract.md`. Этот раздел — обзор, контракт — источник правды.

---

## 7. Как загрузить

1. Открыть `App/1_app.scd` в IDE SuperCollider.
2. Выделить весь файл (Cmd-A) и выполнить (Shift-Return). С умолчаниями это:
   - бутстрапит сервер,
   - аллоцирует шины,
   - грузит 0_lib → synths/* → effects → mixer (LFO/control/sequencer ВЫКЛ),
   - авто-грузит пресет `default` каждого слоя (если есть
     `App/presets/<layer>/default.scd`),
   - открывает ОКНО МИКШЕРА — все слои звучат на уровнях сведения.
3. Остановить звук — **Cmd-.** (безопасно, оставляет чистый сервер).
4. Полная очистка (шины/Ndef/Tdef/MIDIdef/TempoClock секвенсора/окна) —
   выполнить нижний абзац `// CLEANUP` в `1_app.scd`, либо
   `~appCleanup.value(true)`.

**Окна вручную:** `~openMixer.value` (микшер), `~openTuner.(\alpha)`
(отстройка слоя), `~openViz.value` (виз seq+LFO).

**Режимы через `~cfg_app`:**
- только тестируемые слои: `~cfg_app.layers = [\alpha, \theta]` (nil = все);
- включить автоматизацию: `~cfg_app.enableLfo = true`;
- включить таймлайн: `~cfg_app.enableSequencer = true` (+ `autostart = true`
  для авто-старта, либо вручную `~seqStart.value` / `~seqStop.value`);
- включить MIDI: `~cfg_app.enableControl = true`;
- виз-окно: `~cfg_app.enableVizGui = true` (нужно с enableLfo/enableSequencer).

---

## 8. Как итерировать каждый модуль отдельно

Любой файл идемпотентен при перезагрузке (Ndef/Tdef/MIDIdef-ключи
заменяются, не стакаются; LFO чистит исчезнувшие строки по реестру
`~lfoActive`; секвенсор переиспользует свой TempoClock). Поэтому:

- **Один синт-слой** (например `beta`): открыть `App/synths/beta.scd`,
  поправить CONFIG и/или тело `Ndef(\beta)`, выполнить весь файл (Cmd-A,
  Shift-Return) при работающем приложении. Новый звук подменится с
  `fadeTime`-кроссфейдом; маршрутизация микшера сохранится.
- **Сведение**: править таблицу в `2_mixer.scd`, перевыполнить файл — или
  на лету `~mixSet.(\alpha, \vol, 0.5)` / `~mixSet.(\theta, \send, 0.8)`
  / `~masterSet.(0.8)`.
- **Отстройка одного слоя в полном тракте** (через реверб + мастер): сначала
  `~seqStop.value` (чтобы секвенсор не перебивал громкости), затем
  `~mixSolo.(\alpha)` — звучит только этот слой, остальные глушатся
  (`vol=0`, `send=0`). Крутить параметры через `Ndef(\alpha).set(...)` и
  `~mixSet.(...)`. Вернуть всех — `~mixUnsolo.value` (восстанавливает уровни
  из `~cfg_mixer`; ни solo, ни прогон секвенсора исходное сведение не
  перетирают). Чтобы слой не «голым» стартовал, а через тракт, проект
  запускают целиком с `~cfg_app.autostart = false` (см. §7).
- **Отстройка по измеренному референсу**: после правки слоя —
  `.claude/Tools/render_measure.sh <layer> [сек] [пресет|-]`: рендер слоя в
  изоляции + astats/crest/полосные RMS; сравнивать с таблицей «Acceptance
  metrics» в `docs/task.md` (так доводилась `zeta` по
  `docs/zeta_texture_reference.md`).
- **Автоматизация**: добавить строку в `~cfg_lfo.lines`, перевыполнить
  `3_lfo.scd`.
- **MIDI**: добавить строку в `~cfg_ctrl.map`, перевыполнить `5_control.scd`.
- **Таймлайн**: править `~cfg_seq.timeline`, перевыполнить `4_sequencer.scd`,
  затем `~seqStart.value`.
- **Эффекты**: править `~cfg_effects`, перевыполнить `6_effects.scd`.

Где что менять — по таблице §2 и по конвенции CONFIG (§6).

---

## 9. Соответствие восьми звуковым классам (§C reference_v2)

| Класс | Файл | Премиса синтеза | Статус |
|-------|------|-----------------|--------|
| `alpha`   | `synths/alpha.scd`   | §C.1 sustained sine (здесь — порт физмодели «связка пружин в масле» из теста, тоны в MIDI-нотах) | **реальный** (панель, 27 параметров) |
| `beta`    | `synths/beta.scd`    | §C.2 мягкое гранулярное облако коротких синус-зёрен, HPF 200 Hz | заглушка |
| `gamma`   | `synths/gamma.scd`   | §C.3 счётное облако зёрен на партиалах 5/7/9/11/13 × fund | заглушка |
| `delta`   | `synths/delta.scd`   | §C.4 формантный кластер: PinkNoise через 4 BPF с медленной AM | заглушка |
| `epsilon` | `synths/epsilon.scd` | §C.5 allpass-диффузия → дрейфующий CombC → хорус-шиммер | заглушка |
| `zeta`    | `synths/zeta.scd`    | поверхность винила/ленты по измеренному референсу (`docs/zeta_texture_reference.md`); вытесняет §C.6 «dry granulated noise burst» — редизайн 2026-06 | **реальный** (панель, 26 параметров) |
| `eta`     | `synths/eta.scd`     | §C.7 композированная тишина — нет UGen'ов (`Silent.ar`); длительность держит секвенсор | **реальный** (пустой) |
| `theta`   | `synths/theta.scd`   | §C.8 9-голосный ультразвуковой шиммер 12–19 kHz, Splay | заглушка |

Оставшиеся заглушки (`beta`, `gamma`, `delta`, `epsilon`, `theta`) помечены
`// TODO: iterate this layer` и минимальны, но СЛЫШИМЫ и верны премисе синтеза
своего класса. `alpha` портирован полностью из `Tests/test_monade.scd`
(LOCKED — только чтение) и управляется панелью `~spec_alpha`. `zeta`
редизайнен 2026-06 из заглушки в полноценный слой с панелью `~spec_zeta`
(группы `\freq`/`\tone`, рандом + пресеты, доводка циклом
рендер-измерение — см. Приложение B). Шаблон панели общий (0_lib), так что
доводка следующего слоя = написать его `~spec_<class>` по
`docs/layer_contract.md`.

---

## 10. DSP-гигиена

- `Lag`/`.lag` на всех управляемых параметрах, способных прыгать (amp, freq,
  send, rq, частоты LFO и т.д.).
- `LeakDC` после суммирующих/нелинейных стадий (в каждом слое и в мастере).
- Частоты осцилляторов и фильтров клампятся ниже Найквиста ПОСЛЕ lag
  (`….lag(0.2).clip(lo, SampleRate.ir * 0.45)`) везде, где частота
  управляема пользователем.
- Мастер: `Limiter(0.95)` (§K.4) после `LeakDC`.
- `theta` строго 12–19 kHz (клампится), `LPF(20 kHz)`.
- `zeta`: все управляемые частоты (`bedHpf`/`bedLpf`/`bedHumpFreq`/`subFreq`/
  `fineHpf`/`popHpf`/`bodyFreq`) клампятся post-lag ниже Найквиста; высокий
  крест-фактор страхуется внутренним `Limiter(0.95)`; панорама — `±0.4` (§F).
- Идемпотентная очистка: `Tdef/Pdef/MIDIdef/Ndef.all`, освобождение шин,
  секвенсорного TempoClock, `s.freeAll`. После Cmd-. и после `~appCleanup` —
  нет осиротевших узлов и клоков.

---

## Приложение A. Разбор слоя `alpha` (сигнал-флоу)

`Ndef(\alpha)` (`App/synths/alpha.scd`) — физмодель «связка из 10 пружин
в масле», портированная из `Tests/test_monade.scd`. Та же DSP-логика, но как
JITLib-слой (без gate-огибающей, без собственного посыла в эффекты).
Числа ниже — ДЕФОЛТЫ `~spec_alpha`; фактические значения задаёт пресет
`presets/alpha/default.scd` (авто-грузится) и панель `~openTuner.(\alpha)`.

```
ИСТОЧНИКИ ВОЗБУЖДЕНИЯ
                    sway = LFNoise2(swayRate, деф. 0.1) ── общий «вдох/выдох»
                              │ модулирует density и общий уровень
                              ▼
  Dust2(density·sway) ──► contacts ── случайные импульсы соприкосновений
        │                     │
        │   ┌─ ticks: Decay2 → ×BrownNoise → HPF(bright, деф. 2.8к) ┐ «сухой
        │   │  + metal: Mix(9×Ringz 413…7639 Hz, детюн LFNoise0)    │ контакт»
        │   │    × metalAmt; звон mRing=metalRing×профиль;          │ (яркий,
        │   │    + mThump: Ringz(132 Hz)×metalWeight — вес удара    │ без низа)
        │   └─── (ticks·0.7 + metal) × contact ─────────────────────┘
        │
  BrownNoise(bodyLevel) ──► body ── непрерывный низкий гул (масса связки)

РЕЗОНАТОР — Array.fill(10): по одной «пружине» на тон
  для каждой пружины i:
     нота n_i ──.midicps──► sf (частота, Гц; clip 20..2400)
     voice = (body + contacts·collGain) · случ.амплитуда · 0.05/sd
        │      collGain = sf.linlin(110,430, 0.08,0.8)
        ├─ ×3 AllpassC  ← дисперсия (disperse): металлический «зуммер»
        │
        └─ CombC(delay = 1/sf ± oil-джиттер, decay = sd·(1−oil·0.55))
                │  (1/sf даёт высоту; sd = decay·(0.7+i·0.055) — время звона)
                └─ × ringGain = sf.linexp(45,700, 1.5,0.65)
                     (низкие пружины громче → фундамент)

СУММА И ОБРАБОТКА
  bundle = Mix(10 пружин)/10 · sprGain (деф. 4.6)  +  ticks·0.9
        ▼
  LPF(cutBase = cutTop − oil·cutOil, деф. 5400 − oil·3800; clip 300..9000)
        ▼
  ×drive (деф. 1.2) → tanh (мягкое насыщение) → LeakDC

СТЕРЕО (приглушение дальнего конца связки)
  sigR = bundle                                   ← правый: сухой
  sigL = LPF(·(1−muffle·0.82)) → DelayC → ×(1−muffle·0.4) ← левый: глуше
        ▼
  оба × amp (деф. 0.30) × (0.78 + sway·0.22)  ← уровень + дыхание
        ▼
  [sigL, sigR]  ──► выход Ndef (его читает 2_mixer.scd)
```

### Три источника
1. **`Dust2`** — случайные импульсы соприкосновений пружин (плотность =
   `density`, модулирована `sway`).
2. **`ticks` / `metal`** — из тех же импульсов: яркий «сухой контакт».
   `HPF(bright)` убирает низ; модальный банк из 9 `Ringz` (413…7639 Гц,
   высокий спектральный центр = «металл», не «дерево») с независимым
   детюном; высокие моды громче и звенят дольше (`metalRing`); низ — только
   короткий толчок веса удара (`metalWeight`, Ringz 132 Гц). Общая
   громкость = `contact`, доля звона = `metalAmt`.
3. **`body`** — непрерывный `BrownNoise(bodyLevel)`: гул массы связки.

### Сердце тембра — 10 пружин
Цикл `Array.fill(10)`: каждая пружина возбуждается источниками и проходит
- каскад `AllpassC` ×3 — **дисперсия** (`disperse`), металлический «зуммер»;
- `CombC` — задаёт **высоту тона** (задержка `1/sf`) и **звон** (`decay`).
Низкие пружины громче (`ringGain`) — держат фундамент.

### Среда и пространство
- **`oil`** (масло) — опускает срез `LPF` (затемнение, глубина = `cutOil`)
  и укорачивает звон Comb.
- **`muffle`** — приглушает левый канал (фильтр + задержка) → эффект глубины
  / удаления дальнего конца связки.
- Финал: `drive`→`tanh` (мягкое насыщение) → `LeakDC` → стерео × `amp` ×
  дыхание.

### Отличия от теста `Tests/test_monade.scd`
- Нет gate-огибающей `Env.asr` — включение/выключение делает микшер через
  `Ndef(\alpha).fadeTime = 6` (идиома JITLib «слой вкл/выкл», не play/release).
- Нет собственного `Out.ar(fx_bus,…)` — слой только возвращает `[sigL, sigR]`;
  посыл в реверб делает `2_mixer.scd` (§4).
- «Магические числа» теста вынесены в параметры spec (`bright`, `metalAmt`,
  `metalRing`, `metalWeight`, `bodyLevel`, `drive`, `sprGain`, `swayRate`,
  `cutTop`, `cutOil`) — итого 27 управляемых параметров.
- Параметры идут через `Ndef(\alpha).set(...)` из spec/пресета, их же
  дёргают `3_lfo.scd` (LFO) и `5_control.scd` (MIDI).

---

## Приложение B. Разбор слоя `zeta` (vinyl/tape поверхность)

`Ndef(\zeta)` (`App/synths/zeta.scd`) — тихая «поверхность»: шум изношенного
винила / старой ленты. Редизайн 2026-06 по ИЗМЕРЕННОМУ референсу
`docs/zeta_texture_reference.md` (ffmpeg-анализ `Texture.wav`: полосные RMS,
crest 53, плотности щелчков), спека — `docs/task.md`. Доводка велась циклом
рендер-измерение (`.claude/Tools/render_measure.sh zeta`); осознанные
отклонения от спеки (например, `bedLpf` 7000 → 11000 — непрерывный HF-хисс
ленты) — предмет раздела «Post-tuning deviations» в `docs/task.md`.

Панель — по образцу `alpha`: единый источник правды `~spec_zeta`
(26 параметров) → слайдеры (`~openTuner.(\zeta)`), два рандомайзера и
round-trip-пресеты (`presets/zeta/default.scd`; старые пресеты прежней
ВЧ-грейн-зеты — в `archives/zeta/`).

```
СТРАТ 1 — БЭД (непрерывная «поверхность»)
  PinkNoise ── HPF(bedHpf, деф. 270) ── LPF(bedLpf, деф. 11000)
        │
        ├── Resonz(bedHumpFreq деф. 1200, bedHumpRq) ── горб энергии ~1.2 кГц
        │        подмешан XFade2 по bedHumpAmt (0 → чистая полоса)
        ▼
  × wow-дрейф: LFNoise2(bedDriftRate).range(1−bedDriftDepth, 1) × bedAmp

СТРАТ 1b — САБ-РОКОТ («мотор/тонарм», опционален)
  BrownNoise ── LPF(subFreq, деф. 120) × subAmt (деф. 0 — выкл)

СТРАТ 2a — МЕЛКИЙ ТРЕСК («жарящаяся» текстура)
  Dust2(fineRate, деф. 45/с) ── TExpRand(fineAmpMin..1)  ← эксп. амплитуда:
        │                                            много мелких, мало крупных
        ▼
  Decay2(0.0002, fineDecay деф. 1.5 мс) × WhiteNoise ── HPF(fineHpf) × fineAmt

СТРАТ 2b — ПОП (редкие щелчки до почти-пика)
  Dust2(popRate, деф. 2.4/с) ── popLvl = TExpRand(popAmpMin..1)
        ├── click: Decay2(popDecay деф. 6 мс) × WhiteNoise ── HPF(popHpf)
        └── body («thock»): Ringz(bodyFreq деф. 180, bodyRing)
                 × bodyAmt × popLvl²   ← тело только у самых громких поп
        ▼  (click + body) × popAmt

СУММА И КОНДИЦИОНИРОВАНИЕ
  bed + sub + fine + pops ── Pan2(LFNoise1(panRate)·±panWidth ≤ 0.4, §F)
        ▼
  × amp ── LeakDC ── Limiter(0.95)  ← страховка: совпадение поп+тик
        ▼
  стерео ──► выход Ndef (его читает 2_mixer.scd)
```

### Две группы параметров (spec)
- group `\freq` — **частотные составляющие** (рандом «частоты»): у шума нет
  «нот», их роль играют края/центр полосы бэда (`bedHpf`/`bedLpf`/
  `bedHumpFreq`), потолок саба (`subFreq`), HF-наклоны треска (`fineHpf`/
  `popHpf`) и частота «thock»-тела (`bodyFreq`) — 7 параметров, `disp: \hz`
  (рядом со слайдером — ближайшая нота+центы).
- group `\tone` — **текстура/динамика/среда** (рандом «параметры»):
  `amp`, `bedAmp`/`bedHumpRq`/`bedHumpAmt`/`bedDriftRate`/`bedDriftDepth`,
  `subAmt`, `fineRate`/`fineDecay`/`fineAmpMin`/`fineAmt`,
  `popRate`/`popDecay`/`popAmpMin`/`popAmt`, `bodyAmt`/`bodyRing`,
  `panWidth`/`panRate` — 19 параметров.

### Характер и режимы
- Определяющее качество — **высокий крест-фактор**: тихий бэд ↔ редкие
  громкие поп (эксп. распределения `TExpRand` в обоих стратах треска).
- Жёсткий потолок спектра — НЕ ярко, без «воздуха» выше 12 кГц.
- Все импульсы живут ВНУТРИ персистентного графа (Dust2/Decay2/Ringz) —
  никаких per-event Synth.
- Reverb send держать НИЗКИМ (сухая «поверхность», не зал);
  `fadeTime = 0.5` (быстрая текстура, не дрон).

---

## Связанные документы

- [`layer_contract.md`](layer_contract.md) — канонический контракт файла слоя + SC/Qt-footguns
- [`rulus.md`](rulus.md) — пайплайны агентов (включая Pipeline 3 «итерация слоя»)
- [`reference_v2.md`](reference_v2.md) — система UNGRUND: аксиомы, §C–§K
- [`zeta_texture_reference.md`](zeta_texture_reference.md) — измеренный референс zeta
- [`task.md`](task.md) — активная DSP-спека (zeta)
- [`decomposition.md`](decomposition.md) — гармония и композиция системы
- [`improvements.md`](improvements.md) — аудит и бэклог улучшений
